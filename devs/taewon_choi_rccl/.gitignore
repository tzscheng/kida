# 수집 데이터 (에피소드 HDF5)
data/
# 빌드 스크립트/부산물 등 PR에 올리지 않을 것들 (temps/ 안에도 .gitignore 있음)
# 실행 파일을 다시 만들려면: ./temps/build.sh [--onefile]
temps/
# 빌드 결과 실행 파일 (standalone이면 심볼릭 링크, onefile이면 ~400MB 파일)
/kida-gui
/kida-gui.py
__pycache__/
.venv/
